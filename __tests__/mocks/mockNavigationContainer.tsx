import { NavigationContainer } from "@react-navigation/native";

function MockNavigationContainer({ children }: any) {
  return <NavigationContainer>{children}</NavigationContainer>;
}

export default MockNavigationContainer;
